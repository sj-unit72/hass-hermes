{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Hermes",
        "description": "Point this at the Hermes voice bridge running on your network.",
        "data": {
          "url": "Bridge URL",
          "api_key": "API key (optional)",
          "model": "Model name",
          "timeout": "Request timeout (seconds)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the Hermes bridge at that URL.",
      "invalid_auth": "The bridge rejected the API key."
    },
    "abort": {
      "already_configured": "This Hermes bridge is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Hermes options",
        "data": {
          "model": "Model name",
          "timeout": "Request timeout (seconds)"
        }
      }
    }
  }
}
